services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=currency_rate_tracker'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=currency_rate_tracker'
    ports:
      - '5432:5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379:6379'
